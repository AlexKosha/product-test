This is a React + TypeScript frontend for the Product API.
It displays products in a responsive Product Card layout, allows filtering by category, and lets users add new products via the backend API.
The UI is fully responsive and styled using CSS modules, with modern UX practices for clean design.
